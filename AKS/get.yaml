apiVersion: apps/v1
kind: Deployment

metadata:
  name: get-deployment

spec:

  replicas: 2

  selector:
    matchLabels:
      app: get

  template:

    metadata:

      labels:
        app: get

    spec:

      containers:

      - name: get

        image: santandevops01012025/todo-get:v2

        ports:

        - containerPort: 8000
        env:
        - name: CONNECTION_STRING
          value: "Driver={ODBC Driver 18 for SQL Server};Server=tcp:meharserver123.database.windows.net,1433;Database=mehardb;Uid=Useradmin;Pwd=Password@12345;Encrypt=yes;TrustServerCertificate=no;Connection Timeout=30;"